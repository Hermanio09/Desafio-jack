apiVersion: v1
kind: Service
metadata:
  name: {{ include "desafio-jack.fullname" . }}
  labels:
    {{- include "desafio-jack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "desafio-jack.selectorLabels" . | nindent 4 }}
