apiVersion: v1
kind: Service
metadata:
  name: {{ include "desafio-chart.fullname" . }}
  labels:
    {{- include "desafio-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "desafio-chart.selectorLabels" . | nindent 4 }}
