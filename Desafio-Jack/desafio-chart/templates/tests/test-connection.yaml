apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "desafio-chart.fullname" . }}-test-connection"
  labels:
    {{- include "desafio-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "desafio-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
